# Playbook Flashcards

The `web-assets` folder includes 38 optimized diagram images and is only about 1.6 MB total. Upload the whole folder along with `index.html`, `styles.css`, and `app.js` to GitHub Pages.
